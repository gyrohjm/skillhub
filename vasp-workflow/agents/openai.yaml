interface:
  display_name: "VASP Workflow"
  short_description: "Research structures and prepare VASP workflows safely"
  default_prompt: "Use $vasp-workflow to research or confirm the starting structure, review all VASP inputs, and prepare a safe cluster submission."
