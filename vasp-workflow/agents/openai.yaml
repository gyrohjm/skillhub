interface:
  display_name: "VASP Workflow"
  short_description: "Run and orchestrate VASP calculation workflows"
  default_prompt: "Use $vasp-workflow to plan and run VASP workflows: research structures, prepare inputs, review submissions, submit Slurm jobs, automate stages, handle bounded recovery, and hand records to the archive layer."
