# {{AGENT_CONTEXT_FILENAME}}

本文件是 Codex、Claude 等 agent 的项目入口。详细科研背景以
`PROJECT_CONTEXT.md` 为准，详细计划放在 `docs/plans/`。

## 信息源

规划或修改科研工作前，先阅读 `PROJECT_CONTEXT.md` 和 `docs/` 下的相关文件。
计划写入 `docs/plans/`，决策与任务日志写入 `docs/records/`。

## 工作规则

- 保留 `raw_data` 原文件，不得原位修改。
- 计算原始数据放在 `raw_data/calculations/<system_slug>/<case_slug>/`。
- 优化前后结构先进入 raw data；仅经明确批准后复制到 `formal_data/structures/<system_slug>/`。
- 可复现转换代码放在 `code/`。
- 未经用户批准和 manifest 登记，不得将产物放入 `formal_data`。
- 目录名和生成文件名使用英文 `lowercase_snake_case`。
- 明确区分证据、假设、推测和结论。
- 不得静默修改科学参数、排除条件、对照或验证标准。
- 重要科学决策记录到 `docs/records/decisions.md`。
- 执行日志放在 `docs/records/task_logs/`。每次出现实质任务状态变化后，向
  `project_log.md` 和当日 `daily/YYYY-MM-DD.md` 追加事实记录；若当日日志不存在，
  按本地日志格式创建。每天结束前完成当日收尾，不得虚构工作内容。
- 项目范围或验收标准变化时更新 `PROJECT_CONTEXT.md`。
- 面向人的 Markdown 正文默认使用中文；代码、JSON key、路径、文件名、命令、
  `INCAR`/`KPOINTS`/`POTCAR` 等专业参数名保持英文。不要逐句中英双写。

## VASP Skill 分工

- 生产任务创建前使用 `computation-design`：定义可证伪假设、observable、对照、收敛测试、验证、误差目标、计算矩阵、资源预算和停止条件。它负责 `docs/plans/calculation_design.json` 与科学设计审核包，不生成 VASP 输入、不提交、不分析、不归档。
- 科学设计 scope 获批后使用 `vasp-workflow`：准备 POSCAR/INCAR/KPOINTS/POTCAR/job.sh，生成 submit review，提交 Slurm，执行 relax/SCF/band/DOS/phonon 编排和有限恢复。它负责 `task_spec.json`、`state.json`、`submission_review.dat`、queue log 和自动化文件。
- 任务存在后使用 `vasp-work-manager`：登记状态、维护 ledger、归档计算目录、生成 manifest 和 SHA256、校验 archive、更新项目摘要。它不得提交任务、恢复运行或修改科学输入。
- 输出完成后使用 `vasp-analysis`：提取可复用 `.dat`、绘图、写分析报告，并按已批准的 observable 和 decision rule 解释结果。新增计算建议必须形成 design change request 返回 `computation-design`。
- 集群 VASP 项目使用 `/home/<user>/projects/<project_slug>/`；`project_slug` 必须是简短英文 `lowercase_snake_case`。
- 集群上不复制本地 `raw_data/`、`formal_data/` 树。输出保留在来源 case，处理结果写入该 case 的 `analysis/`，项目级索引写入 `docs/project_summary.md`。
- 生产计算前完成并批准 `docs/plans/computation_plan.md` 与 `docs/plans/calculation_design.json`。relax、SCF、band、DOS、phonon 等阶段需明确继承参数、override、KPOINTS policy、POTCAR label、结构来源、资源配置和 completion gate。
- relax 收敛后，下游任务可在预审 envelope 内继续；任何超出 envelope 的 INCAR/KPOINTS/POTCAR/结构/资源变化都必须停止并复核。
- 保留 `relax/POSCAR-ini`。未收敛 relax 续算前，先保存当次 `OUTCAR` 和 `CONTCAR`，再将 `CONTCAR` 复制为 `POSCAR`。
- 下游任务复用 SCF 的 `CHGCAR` 或 `WAVECAR` 时使用符号链接 `ln -s`，不得复制大文件。
