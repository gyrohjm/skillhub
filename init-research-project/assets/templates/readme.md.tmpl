# {{PROJECT_NAME}}

- **项目标识：** `{{PROJECT_SLUG}}`
- **类型：** `{{MODE}}`
- **领域：** {{DOMAIN}}

## 项目目标

{{SUMMARY}}

## 目录结构

- `docs/literature/`：论文、来源笔记和阅读队列。
- `docs/plans/`：科研、计算、实验、里程碑和风险计划。
- `docs/records/`：决策与任务日志。每次实质状态变化后更新
  `task_logs/project_log.md` 和当日日志，并在每天结束前完成收尾。
- `code/`：可复用源码、脚本、notebook、测试和配置。
- `raw_data/calculations/<system_slug>/<case_slug>/`：按体系和 case 保存原始计算输入、输出及优化前后结构。
- `raw_data/`：不可修改的来源数据和明确标记的中间数据。
- `formal_data/structures/<system_slug>/`：仅保存用户批准的可复用结构。
- `formal_data/`：经过验证、批准并带 provenance 的正式产物。

## 数据规则

```text
raw_data -> code processing -> validation -> user approval -> formal_data
```

修改科研范围或科学假设前先阅读 `PROJECT_CONTEXT.md`。VASP 项目生产运行前，
在 `docs/plans/computation_plan.md` 和 `docs/plans/calculation_design.json`
中定义并批准计算矩阵。使用 `computation-design` 负责科学设计，
`vasp-workflow` 负责运行编排，`vasp-work-manager` 负责记录归档，
`vasp-analysis` 负责输出处理和解释。

本地项目使用 `raw_data/formal_data` 生命周期；集群输出与 `analysis/` 产物保留在
来源 case，项目级只维护精简的 `docs/project_summary.md` 索引。
