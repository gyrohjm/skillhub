interface:
  display_name: "VASP Analysis"
  short_description: "Process and analyze completed VASP outputs"
  default_prompt: "Use $vasp-analysis to extract validated .dat data, generate figures, interpret completed VASP outputs, and write an analysis report."
