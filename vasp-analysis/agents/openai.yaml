interface:
  display_name: "VASP Analysis"
  short_description: "Process and analyze completed VASP outputs"
  default_prompt: "Use $vasp-analysis to extract validated .dat data, generate figures, evaluate approved design observables, write an analysis report, and return proposed new calculations to $computation-design as a change request."
